# 5차 개선: 독립 평가에서 운영 검증까지

상태: 진행 중. 이슈 #86. 선행 코드 292eb111a99ee7eb2ddff67b65c0e96cda6fa380. LightRAG 미도입.

## 승인된 전체 범위와 증명 조건

1. 새로운 질문·무응답과 원문 근거를 작성·검수하고 평가 파일 해시를 개선 전에 고정한다. 이전 33개 질문 및 동일 의도와 중복을 검사한다. 개발셋과 최종 검증셋은 문서/질문 의도로 분리한다. 같은 작성자가 원문 기반으로 만든 합성 검증은 현장 독립 검증과 구분하며 사람 검수를 대신했다고 주장하지 않는다.
2. 무응답 판정은 개발셋으로만 개선한다. 정상 적중률, 무응답 오탐, 제품·버전·권한 격리, 지연을 함께 측정한다. 검증셋 공개 후 그 결과에 맞춰 튜닝하면 그 세트는 개발용으로 전환하며 새 검증이 필요하다.
3. 원본을 유지하는 후보 생성으로 메뉴와 중복을 제거한다. 제목·절차·표·코드의 보존과 벡터 무효화를 테스트하고 원문/청크 근거 위치를 기록한다. 청킹 전후를 동일 질문으로 비교한다.
4. 실제 장문 다국어 임베딩의 모델/revision/입력/차원을 검증한다. 같은 코퍼스와 평가 조건으로 BM25 및 의미 검색을 비교하고 품질·무응답·비용·지연 근거로 채택한다. 모델명이나 임의 벡터를 실제 추론 결과로 취급하지 않는다.
5. 제품의 실제 답변 경로에서 질문·답변·인용·제품·버전을 평가한다. 문서 적중률과 답변 정확도를 별도 보고한다. 정답 근거 없는 응답과 잘못된 버전 주장은 실패다. 근거 불충분이면 불확실성을 표시한다.
6. 선행 PR과 신규 변경을 검토하고 필수 CI를 통과시킨 후 병합한다. 실제 배포 위치/현재 버전/데이터 권위를 확인하고 검증된 아티팩트를 배포한다. readiness와 실제 검색/답변 read-back을 기록해야 운영 완료다. 기존 서명 승인, 단일 사용 nonce, ACL 및 권위 저장소 게이트는 유지한다.

## 사전 확인

- 2026-09-09: 선행 PR #78/#81/#83/#85는 열려 있다. PR #85 마지막 코드/문서 CI는 통과했다.
- BLRO SSH 연결 확인: hostname blro01. Linux SSH에서 macOS 전용 UseKeychain 옵션만 IgnoreUnknown 처리했다. SSH 인증/호스트 검증은 우회하지 않았다.
- /srv/blro-artifacts 및 /srv/blro-data에는 초기 디렉터리만 확인됐다. 시스템 서비스에서 대상 제품은 아직 찾지 못했다. 사용자 rootless Docker와 ~/sangfor-os, ~/src, ~/deploy가 있어 추가 읽기 확인 중이다. 이 사실만으로 미배포라고 단정하지 않는다.
- 기존 평가 21+12문항은 개발 회귀셋이며 독립 평가로 재사용하지 않는다. 기존 BM25의 추가 무응답 1/4 오탐은 해결해야 할 회귀 사례다.

## 완료 기록

각 단계의 원시 결과, 명령, 코드/데이터 해시와 한계를 완료 시 추가한다. 현재 1–6 완료를 주장할 근거는 없다.

## 진행 증거: 평가 고정 및 무응답 개발 실험

- 836c078에서 새 질문 12개(6제품), 무응답 12개를 고정했다. fixture SHA-256 `7c7be3aa0c31a5ff4bd7bea0a626a9dac8b7eece935ad6bfad6d965030224623`. 이전 평가의 정답 소스와 겹침 0, 모든 정답 인용의 원문 위치·해시 확인. 사람의 독립 검수는 아직 없으며 합성 평가라는 한계를 manifest에 기록했다. 아직 이 세트의 검색 결과는 열지 않았다.
- `SANGFOR_RAG_REQUIRE_SUBJECT_MATCH=1` 실험: 기존 21문항 21/21, 개발 회귀 12문항 12/12 유지. 개발 무응답 오탐 1/4 → 0/4, 기존 무응답 0/4 유지. 요청 표현·제품/버전만 일치하는 결과를 거절한다. 확률 보정된 답변 신뢰도로 해석하면 안 된다. 현재 opt-in 상태이며 최종 승인 전이다.
- 개발 평가 명령: `SANGFOR_RAG_REQUIRE_SUBJECT_MATCH=1 pnpm run rag:eval:corpus <revision-v3 corpus-cleaned.json> <revision-v1-qrels-v2.json 또는 revision-v3-validation.json>`. 설정이 달라 기존 동일 설정 게이트에 비교 보고서를 넣지 않았다. 원시 결과는 docs/references/revision-v5/subject-*.json.
- BLRO의 실제 제품 파일 위치는 `/home/blro/orca/projects/sangfor-engineer-mcp`다. Git checkout이 아닌 파일 복사본이며 기본 3502/3600/3700 포트의 listener를 찾지 못했다. `start-mcp.sh`는 로컬 tsx stdio 서버를 실행한다. 현재 배포 여부는 더 검증해야 한다.

## 진행 증거: 구조 청킹 및 장문 모델

- `pnpm run rag:prepare:structured`로 별도 후보 생성 및 read-back 완료. 18,399개 문서/스코프 그룹, 66,767 → 39,789청크, 10,123개 문서에서 확인된 수집기 chrome 제거, 그룹 내 동일 청크 361개 제거. 텍스트가 바뀐 후보 벡터는 모두 무효화. 원본 유지.
- 구조 후보 개발 실측: 기존 20/21, 추가 12/12, 무응답 각각 0/4. 추가 MRR 0.6597 → 0.8889, 기존 MRR 0.7937 → 0.7222. 기존의 RAID 디스크/노드 격리 라벨 불일치 문항이 top5 밖으로 이동했다. 결과를 숨기거나 qrels를 바꾸지 않았다. 구조 후보 채택은 아직 확정하지 않았다.
- 구조 청킹 4개 및 주제 일치 4개 테스트 통과, 타입 검사 통과. 전체 CI는 아직 미실행.
- E5-small의 512토큰 입력과 query/passsage prefix 요구사항을 [공식 모델 카드](https://huggingface.co/intfloat/multilingual-e5-small/raw/main/README.md)에서 확인했다. 고정 revision `614241f622f53c4eeff9890bdc4f31cfecc418b3`, 384차원 실제 로컬 health 확인. 기존 embedForRole의 query/document prefix를 사용해 구조 후보 전체 재임베딩을 시작했다. 실행 결과와 품질은 아직 미확정이다.

## 진행 증거: 실제 구성 계획의 인용 검증

- 실제 `generateConfigPlanAsync` 경로가 요구사항 텍스트로 문서를 검색하도록 수정했다. 인덱스에서 결과가 없으면 예제 문서로 대체하지 않으며, 제품/버전 불일치·빈 문서·없는 인용 ID를 검증한다.
- 고정 템플릿 구성 계획은 `UNVERIFIED_TEMPLATE`, 근거가 없으면 `INSUFFICIENT_EVIDENCE`, 인용이 잘못되면 `INVALID_REFERENCES`를 반환한다. 구조 검증 `ok`와 답변 검증 `answerReady`를 혼동하지 않는다. 현재 템플릿은 claim 단위 증명이 없어 answerReady=false다.
- 개발 16질문 실제 구성 계획 평가: 인용 무결성 실패 0, answerReady 0. 답변 정확도는 null이다. 이는 정답률 0% 또는 100%가 아니라 해당 경로가 직접 Q&A를 생성하지 않음을 뜻한다. MCP 호출 AI의 최종 답변 평가는 별도 단계로 남는다.
- 관련 기존+신규 14테스트 및 최종 인덱스 무응답 통합 테스트 4개 통과. 통합 테스트의 초기 fixture updatedAt 누락으로 인덱스 파싱이 거절됐으며, 유효한 timestamp를 넣어 fixture를 수정했다. 파서의 거절 조건은 유지했다.
- BLRO 파일 복사본에는 node_modules와 .env가 없고, 해당 디렉터리를 cwd로 사용하는 프로세스도 없다. 운영 갱신 전 현재 정본 실행 위치/클라이언트 경로를 확인해야 한다.

## 진행 증거: MCP 클라이언트 답변과 이웃 근거

- 문서 QA용 `sangfor-answer-from-docs` 프롬프트를 추가했다. 본문 확인, 정확한 filePath/id/인용, 제품·버전 유지, 근거 부족 시 답변 보류를 요구한다. 프롬프트는 호출 AI의 워크플로이며 서버가 LLM 답변을 생성한다고 주장하지 않는다.
- 실제 stdio MCP initialize/tools-list/prompts-get/tools-call 응답을 `rag:capture:mcp`로 캡처했다. 평가 중 검색 gap 기록은 끄고 원본 인덱스는 변경하지 않았다.
- `contextNeighbors` 0–2 옵션은 이미 권한 필터를 통과한 동일 문서/제품/버전/tenant/project/ACL/신뢰도에서 이웃 청크를 제공한다. 원래 hit와 순위를 유지하고 벡터를 추가 전송하지 않으며, 문장을 자르지 않는 8,000자 컨텍스트 한도를 적용한다.
- 제목이 제목 점수와 본문 점수에 중복 반영돼 짧은 주의사항 청크가 실제 절차를 밀어내는 문제를 수정했다. 구조 후보의 기존/개발 적중률은 20/21·12/12 유지, MRR은 0.7183·0.8222. 이전 구조 후보보다 문서 MRR은 조금 낮지만 HA의 실제 Enable/인터페이스 설정 본문을 반환하는 것을 MCP 캡처에서 확인했다. 수치 회귀는 기록하고 감추지 않았다.
- 실제 캡처에서 현재 Codex 클라이언트가 개발 답변 16개를 작성했다. 완성 답변 11개, 문서에 입력 세부사항이 없는 NTP 키 질문은 부분 답변 1개, 무응답 4개는 보류. 모든 인용을 캡처 원문·위치·SHA·제품·버전으로 검증해 무결성 오류 0을 확인했다. 답변 작성자가 근거를 검토했으나 외부 독립 의미 검수는 수행하지 않았다. 인용 무결성을 자동 답변 정답률로 바꾸지 않으며 `semanticAccuracy`는 null이다.
- PR #87의 85304e8 코드: Node 22/24, PostgreSQL, PR 검증 모두 성공. BLRO 별도 릴리스 `/home/blro/orca/projects/sangfor-engineer-mcp/releases/85304e8` 아카이브 SHA는 로컬/원격 모두 `f21f7ea4a589657e3461486311264fac1089cb5947270ffe6ea0cc600c784925`. 의존성·Prisma 생성·lint/build 및 MCP smoke(118 tools) 성공. 기존 운영 파일·실행 경로는 전환하지 않았다. 이후 추가 코드는 재검증 대상이다.
- CodeRabbit 0.7.6 설치 후 미인증을 확인했고 브라우저 로그인을 시작했으나 완료되지 않았다. 로그인 세션은 143으로 종료됐다. CodeRabbit 리뷰는 실행되지 않았으며 수동 검토를 CodeRabbit 결과로 대체하지 않았다.

## 실행 복구 기록

- 최초 구조 E5 전체 재임베딩이 exit 143(SIGTERM)으로 중단됐고 프로세스/후보 파일 부재를 확인했다. 종료 원인은 확인되지 않았다. 완료로 처리하지 않았다.
- `SANGFOR_RAG_REEMBED_CHECKPOINT_DIR`를 추가해 32청크 배치를 원자적 인덱스 쓰기로 보존한다. 재개 시 원본 파일 해시, 모델/revision 식별에 따른 디렉터리와 청크 원문·스코프·벡터 공간을 검증한다. 손상/재라벨링은 거절하고 최종 파일은 전체 완료 후에만 발행한다. 실제 CLI 33청크 실행 후 두 번째 실행에서 33개 전부 재개됨을 테스트했다.
- E5는 이 방식으로 다시 실행 중이다. MiniLM 전체 후보는 생성됐다. `rag:verify:reembed`로 전체 메타데이터/공간/정규화 및 실제 모델의 4개 위치 재계산을 확인한다.

## 진행 증거: API 규격 회귀 수정 및 언어별 실측

- 8609a14 전체 CI는 3,258개 테스트 통과, MCP 표면 규격 1개 실패였다. 새로운 선택 입력 contextNeighbors와 QA 프롬프트를 기존 불변 baseline에 덮어쓰지 않고 명시적인 v5 차이로 검증하도록 수정했다. 범위 0–2를 초과하는 변경은 거절함을 확인했다. 관련 18테스트 및 lint 통과, ab87fe9 푸시 후 전체 CI 재실행 중이다.
- 실제 MiniLM 39,789청크 후보에 동일 설정(주제 일치 켬, reranker 끔)을 사용한 개발 비교를 완료했다. dense: 기존 14/21·개발 11/12, hybrid alpha=.5: 21/21·12/12, RRF: 19/21·11/12. 모든 모드에서 기존/개발 무응답 각 0/4. 원시 6개 보고서는 minilm-{dense,hybrid,rrf}-{original,development}.json이다. hybrid MRR은 .6548·.6139로 문서 발견률과 최상위 순위 품질을 구분해야 한다.
- 기존 영어 개발 질문의 한국어 번역 12개와 무응답 4개를 별도 개발 fixture에 기록했다. 원본 의도/정답 라벨은 유지했으며 신규 독립 평가로 계산하지 않는다. 고정 holdout은 아직 열지 않았다.
- 한국어 MiniLM hybrid에서 주제 일치 켬: 3/12, 무응답 오탐 0/4. 끔: 8/12, 무응답 오탐 4/4. 영어 위주의 어휘 조건을 단순 제거하는 것으로는 한국어 답변 신뢰성을 해결하지 못한다. 현재 설정 운영 승격은 보류하며 E5 동일 실험과 언어 대응을 완료해야 한다. 해당 결과는 minilm-hybrid-korean*.json에 보존했다.

## 진행 증거: 클라이언트 번역 검색

- 서버에 임의 번역 사전을 추가하지 않고 실제 MCP 문서 QA 프롬프트가 비영어 질문의 충실한 영어 번역을 원문과 함께 query에 넣도록 했다. 부정, 식별자, 제품/버전, 불확실성을 유지하고 정답/추측 기능/문서 제목을 추가하지 않도록 명시했다. 이는 호출 AI의 업무이며 서버 자동 번역 기능이 아니다.
- 현재 Codex가 한국어 개발 질문만을 뜻에 맞게 번역해 `revision-v5-client-korean-development.json`에 원문/번역/결합 입력을 기록했다. 노출된 개발셋이며 독립 평가로 계산하지 않는다. MiniLM hybrid 동일 설정: 11/12 적중(MRR .5792), 무응답 오탐 0/4. 원어 영어 질문 12/12보다 1문항 낮으며, 한국어 단독 검색 해결로 보고하지 않는다.
- 실제 stdio MCP로 16개 결합 질문을 호출해 변경된 프롬프트와 검색 응답을 캡처했다. `mcp-client-korean-development.json`은 실제 도구 응답이며 최종 답변 생성 결과는 아니다. 관련 MCP 표면/프롬프트 18테스트 통과.

## 선택 절차 고정

- E5 개발 비교와 sealed holdout 공개 전에 `development-selection-policy.json`으로 선택 조건을 고정했다. 기존 적중률 95%, 영어 개발/클라이언트 번역 한국어 각 90%, 세트별 무응답 오탐 10% 이하, 금지 소스 0을 요구한다. 통과 후보 중 최저 적중률, 평균 MRR, p95 순으로 비교한다. 한국어 원문 단독 성능은 별도 공개하며 자동 번역 서버 성능으로 바꾸어 보고하지 않는다.
- 번역 포함 한국어 개발에서 구조 BM25도 11/12, 무응답 0/4, MRR .7389였다. MiniLM hybrid의 같은 입력 MRR .5792보다 높다. E5 완료 전 채택하지 않는다.
- 완료된 E5 파일에 전체 모델 read-back을 먼저 실행하고, 같은 네 개발 fixture로 dense/hybrid/RRF를 순서대로 평가하는 일회 실행 작업을 시작했다. 해당 작업은 현재 확인한 임베딩 PID만 관찰하며 후보 없이 종료되면 중단한다. 모델을 재시작하거나 holdout에 접근하지 않는다.

## 최종 후보 검증 실패 및 후속 개선

- E5 39,789청크 전체 생성 및 4위치 실제 재계산 검증 완료. 후보 SHA `ff1118e44d4c1f21e7e465f6e1d66fcb0f4a08a09e9f1f7fbc098054c3febfa0`. 전체 원문/메타데이터·벡터 공간·정규화 검증 통과. 모델/revision은 readback 보고서에 고정돼 있다.
- 고정 선택 규칙으로 BM25를 후보로 선택하고 0ae2c2c에서 평가 전에 기록했다. 세 적격 후보의 최소 적중률 .9167은 같았고 평균 MRR은 BM25 .7598, E5 hybrid .6741, MiniLM hybrid .6159였다. BM25의 기존 20/21 회귀는 숨기지 않았다.
- 첫 holdout 평가: 원문 8/12, 무응답 오탐 0/12. 클라이언트 번역 포함 10/12, 무응답 오탐 6/12. 금지 소스는 모두 0. 목표 90%/10%를 충족하지 못해 `NOT_ELIGIBLE`로 판정했으며 운영 승격하지 않았다.
- 기존 holdout과 번역본은 이 시점부터 노출된 개발용 자료다. 파일/라벨/최초 결과는 보존한다. 이 질문으로 개선한 뒤 같은 결과를 독립 검증이라고 부르지 않으며 새 평가셋이 필요하다.
- 번역 후 누락은 NGFW 업그레이드 재시작 중단과 SCP 업그레이드 후 서비스 검증 문항이었다. 허구 기능 6개는 공통 기술 단어만으로 문서를 반환했다. 기존 주제 검사만으로 충분한 답변 근거가 있음을 증명할 수 없었다.
- 기존 캐시의 로컬 mmarco cross-encoder를 시작하고 E5 후보 40개에 대한 실제 재정렬 점수를 수집 중이다. 순위와 근거 부족 분리 가능성을 먼저 검증하며 아직 임계값이나 운영 필터를 추가하지 않았다.
- BLRO에는 별도 검증용 E5 서비스를 loopback 8104로 실행했다. 로컬과 동일한 torch/sentence-transformers/transformers/FastAPI/uvicorn 버전의 의존성 검사 통과, 고정 revision/384차원 health 확인. Python은 BLRO 3.14.4, 로컬 3.12.13이다. 모델 캐시 아카이브 SHA `238605b40749a624de236c17f315aef08b846947965e546cea25e04e3f7b0be8` 전송 검증. 이 서비스 준비는 최종 운영 모델 채택이 아니다.
- Cursor Bugbot은 사용자/팀 사용량 한도로 실패했다(check run 102321879747). 리뷰 통과로 해석하지 않는다. CodeRabbit CLI도 최근 확인 시 경로에 없어 현재 실행 가능/인증 상태를 주장하지 않는다.

## 후속 진단과 두 번째 최종 평가 고정

- 2805d30의 문장부호 수정 CI는 verify, validate, PostgreSQL authority, JM endpoint 모두 통과했다. 이 결과는 후속 변경의 CI나 최종 품질 통과를 대신하지 않는다.
- 상위 제목 필드를 추가하는 개발 실험은 단독으로 두 누락을 복구하지 못했다. [stemmer 2.0.1](https://github.com/words/stemmer)의 Porter 구현을 무결성 확인 후 격리된 실험 디렉터리에서 사용했다. 제품 의존성이나 기본 검색 동작에는 아직 추가하지 않았다. 단어 형태 처리와 상위 제목을 E5 hybrid에 함께 적용하면 노출된 개발 질문의 후보 40개 내 발견은 12/12가 되지만, 기존 번역 한국어 개발 Hit@5는 11/12에서 10/12로 낮아진다. 그대로 채택하지 않는다. 원시 비교는 ancestor-field-experiment.json, stem-field-experiment.json, stem-hybrid-experiment.json에 보존했다.
- 설정 해시를 검증한 Qwen3-Reranker-0.6B 실제 추론으로 기존 21문항과 노출된 12문항을 진단했다. 상위 검색 후보 3개와, 누락된 경우 별도로 표시한 정답 원문을 입력했다. `oracleOnly` 결과는 검색 적중으로 계산하지 않는다. 6개 허구 기능의 상위 3후보 점수는 낮았지만, 실제 정답 근거가 없는 관련 문서에도 높은 점수가 있었다. 이 진단은 전체 후보 평가나 의미 정확도 증명이 아니다. 점수 임계값을 운영에 적용하지 않았다.
- 새 최종 평가 12개와 무응답 12개를 별도로 작성했다. 질문/번역/정답 인용을 검색 전에 고정했다. 원문과 구조 후보에서 인용 12개를 모두 검증했고 이전 정답 파일과 겹침은 0이다. 질문 의도는 기존 목록과 대조했으며 제품/버전 범위는 명시적 원문 메타데이터만 사용했다. IAG 지원 사례에는 버전 필터를 추정해 넣지 않았다.
- 원문 fixture SHA `a2ddf26c7976c4ae9429944af049e901aa0760203656a3915cc036eb50d9b3e3`, 클라이언트 번역 포함 fixture SHA `58c2208f559877af0bedf54f5768f1c15522646368378afdf04e1258e58e91d0`. `holdout-2-manifest.json`에 원문 위치/해시와 작성·표집 한계를 기록했다. 두 파일 모두 평가 입력 스키마 검증을 통과했다. 새 질문의 검색/모델 평가 결과는 아직 열지 않았다. 사람의 독립 검수나 실제 현장 무작위 표본으로 주장하지 않는다.

## 실제 후보 40개 처리 및 평가 식별 보완

- E5·단어 형태·상위 제목으로 만든 실제 후보 40개를 동일 설정 Qwen에 입력했다. 누락되던 NGFW 정답은 38위 후보에서 재정렬 2위(40.97초), SCP 정답은 5위 후보에서 재정렬 1위(56.65초)가 됐다. 정답을 후보에 삽입하지 않았다. 이는 두 개발 문항의 진단이며 전체 품질/운영 지연 통과가 아니다. `qwen-full-pool-diagnostic.json` 참조.
- 같은 후보 40개 조건의 허구 기능 진단에서 첫 세 요청은 각각 60초 시간 초과로 점수를 얻지 못했다. 이를 무응답 성공이나 오탐 0으로 해석하지 않는다. 현재 40개/1024토큰 설정은 운영 후보로 승인하지 않는다. 다음에는 요청이 취소돼도 서버 추론이 계속될 수 있는 상황과 후보·입력 예산을 함께 점검해야 한다.
- 평가 CLI가 재정렬의 모델/revision만 기록하고 지시문·입력 길이·계산 형식·배치 크기 변경은 설정 해시에 반영하지 않는 누락을 수정했다. 실제 클라이언트가 사용하는 설정 해시를 보고서에 포함하고 후보 수/요청 제한도 구분한다. 지시문 원문은 저장하지 않는다. 이 값은 구성 식별이며 추론 성공을 증명하지 않는다.
- 보고서/품질 비교/재정렬 설정 관련 35개 테스트 통과. 새 테스트의 객체 배열 타입 추론 오류를 명시적 타입으로 수정한 뒤 `pnpm run lint`도 통과했다. 지시문이나 후보 예산만 바꿔도 서로 다른 설정으로 기록되는 것을 실제 평가 CLI 출력으로 확인했다. 단일 후보 fixture를 사용한 이 테스트는 모델 성능 측정이 아니다.
- 허구 기능 6문항의 전체 후보 진단이 종료됐다. 5문항은 timeout으로 점수를 얻지 못했고, NDR 문항만 후보 2개를 7.95초에 처리했다. 마지막 시간 초과 중 하나의 관측 경과는 246.65초였다. 누락 점수는 null/빈 배열로 보존하며 오탐률을 산출하지 않는다. `qwen-full-negative-diagnostic.json` 참조.
- 시간 초과 후 로컬 호스트에서 RAM 124,366MiB 중 available 3,239MiB, swap 4,095MiB 전부 사용, load average 63.19/79.43/48.77을 관측했다. 요청 취소 후 서버 작업 지속 여부도 아직 계측하지 않았다. 이 결과를 모델 고유 지연으로 일반화하지 않는다. 사용이 끝난 소유 테스트 서버 8005(mMARCO)와 8007(기본 Qwen)은 정확한 PID/실행 경로/포트를 확인하고 종료했다. 다른 사용자의 작업은 변경하지 않았다. 후속 전체 후보 평가는 자원 상태를 확인한 BLRO 별도 검증 환경과 서버 동시 요청 제한을 먼저 확인한 뒤 진행한다.

## BLRO 재측정과 선택형 후보 확장 구현

- BLRO에서 가용 RAM 약 25GB와 낮은 부하를 확인하고 `/home/blro/orca/projects/sangfor-engineer-mcp/experiments/qwen-38214cf`에 별도 loopback 모델 서비스를 준비했다. 모델 캐시 아카이브의 로컬/원격 SHA는 `45c9d6710416f4977d3267a2667766e11553d1eb10438a5dd762040c414b62a1`로 일치했다. 모델·revision·설정 해시와 실제 설치 패키지 버전은 `blro-qwen-environment.json`에 기록했다. 운영 경로 전환은 하지 않았다.
- 동일한 허구 기능 6문항과 최대 40개 후보 재측정은 모두 완료됐다. 소요 시간은 58.24/59.07/49.66/2.89/36.79/56.80초, 최고 점수는 -0.80/3.21/2.72/0.28/4.73/3.30이었다. NDR 후보는 2개, IAG 후보는 25개이며 나머지는 40개다. `blro-qwen-full-negative-diagnostic.json`은 실제 입력과 해시/점수를 보존한다. 높은 점수의 허구 질문이 남고 지연도 길어 최종 채택이나 답변 정확도 통과를 주장하지 않는다.
- 서버는 한 프로세스에서 추론 하나만 허용하고 겹친 요청은 대기열에 쌓지 않고 503으로 거절하도록 보완했다. 클라이언트 취소 자체가 CPU 추론을 취소하지는 않으므로, 기존 추론이 끝날 때까지 슬롯을 유지한다. 동시 요청/예외 후 해제의 표준 라이브러리 테스트 2개를 로컬과 BLRO에서 통과했고 CI에도 연결했다. 실제 Qwen HTTP 검사에서도 busy 상태 중 두 번째 요청이 0.0022초에 503으로 거절됐으며, 첫 요청 종료 후 새 요청은 정상 처리됐다. `blro-reranker-admission-readback.json` 참조.
- `SANGFOR_RAG_LEXICAL_PROFILE=stem-ancestors` 선택형 구현을 추가했다. 기본값 exact는 유지하며 알 수 없는 프로필은 거절한다. `stemmer@2.0.1`을 고정하고 일반 영단어의 형태와 상위 제목을 검색 점수에만 반영한다. 원문·벡터와 정확 어휘 기반 주제 검사는 유지한다. 평가 구현 해시에는 의존성 파일도 포함한다.
- 실제 구현/E5 개발 평가: 기존 21/21, 영어 12/12, 번역 한국어 10/12, 노출된 이전 holdout 11/12. 무응답 오탐은 각각 0/4, 0/4, 0/4, 6/12였다. 모든 결과의 degradation·금지 소스·누락 정답 소스는 0이다. 번역 한국어가 목표에 미달하므로 이 설정 단독으로는 승인하지 않는다. 원시 결과는 implemented-profile-*.json이며 새 holdout-2는 열지 않았다.
- 관련 TypeScript 테스트 94개와 타입 검사 통과. 전체 재정렬 및 답변 근거 검증이 남아 있으며, 다음 개발 평가는 실제 후보와 모든 원점수를 기록해 임계값 적용 시의 누락/오탐을 구분한다.

## 선택형 원점수 필터의 오류/무응답 분리

- `SANGFOR_LOCAL_RERANK_MIN_SCORE`를 명시했을 때만 로컬 비동기 검색에서 원점수 필터를 적용하도록 구현했다. 기본 임계값은 없으며 운영 값은 아직 선택하지 않았다. 고정된 모델/revision/추론 설정을 가진 로컬 scorer가 필요하다. 평가 설정 해시에 임계값을 별도로 기록한다.
- 모든 후보의 점수를 받아야 하며 후보 하나도 건너뛰지 않는다. 기준 미달 문서를 기존 검색 순서로 다시 채우지 않는다. 채택된 문서의 rerankScore에는 실제 원점수를 보존한다. 모든 점수를 정상 수신했으나 기준 미달인 경우에만 빈 결과를 반환할 수 있다.
- 모델 오류·503 busy·시간 초과·설정 불일치·부분/잘못된 점수 응답은 `RAG_SCORE_GATE_UNAVAILABLE`로 실패한다. MCP의 기본 응답이 배열이므로 빈 성공으로 반환하면 호출자가 장애와 무응답을 구분할 수 없기 때문이다. 의도하지 않은 임베딩 fallback과 동기 검색 우회도 거절한다. 기존의 필터 미사용 경로는 기존 fallback 계약을 유지한다.
- 관련 85개 테스트와 타입 검사 통과. 테스트의 잘못된 권한 거절 사유 리터럴은 실제 계약의 SCOPE_INVALID로 수정했다. 단위 테스트용 점수 4를 모델 전체의 보정된 기준으로 해석하지 않는다. 새 최종 평가셋은 아직 열지 않았으며, 전체 개발 질문의 실제 후보/점수 평가 후 기준을 고정해야 한다.

## 전체 점수 비교 및 CI 경계 보완

- 영어 개발 12개와 무응답 4개의 전체 후보 추론이 완료됐다. `full-calibration-english.json`에 원점수를 보존했다. 모델 점수 순서는 10/12이며, 점수 하한 3~3.5에서 검색 순서를 유지한 오프라인 재평가는 12/12다. 개발 결과이며 새 보류 평가나 최종 설정 승인이 아니다.
- `b180f3e` CI에서 3,310개 테스트가 통과했으나 검색 모듈 크기 검사 1건이 실패했다. 제한을 늘리지 않고 진단 계산을 `rag-search-diagnostics.ts`로 분리했다. 새 모듈도 동일 크기 검사의 대상에 포함했으며 평가 구현 해시에 추가했다. 관련 46개 테스트와 타입 검사를 통과한 `f095282`를 푸시했고 원격 전체 검증은 다시 진행 중이다.
- 실제 stdio MCP에서 존재하지 않는 버전의 질문은 성공한 빈 결과로, 활성 Qwen 추론과 겹친 요청은 `RAG_SCORE_GATE_UNAVAILABLE` 오류로 반환됨을 확인했다. 시험용 하한 3.5는 최종 설정이 아니다. 첫 진단은 잘못된 환경 변수로 기본 엔드포인트를 사용했으므로 제외하고, 올바른 URL과 모델/revision/busy 상태를 확인한 재실행만 `mcp-score-gate-busy-readback.json`에 기록했다.
- 한국어 시스템 로그 질문은 현재 후보 40개에 정답이 없다. 원문을 유지하고 syslog 용어를 추가한 개발 진단에서는 정답이 1위가 됐다. `system-log-alias-diagnostic.json`은 개발 진단이며 아직 기본 검색 동작 변경이나 최종 성능을 증명하지 않는다.

- 한국어 개발 12개와 무응답 4개의 전체 추론은 모두 오류 없이 끝났다. `full-calibration-client-korean.json`에 기록했다. 하한 3.3/3.5와 검색 순서 유지에서는 10/12로 기준에 미달한다. syslog 절 누락과 낮은 점수로 제외되는 실시간 마이그레이션 질문을 구분했다.
- 원래 질문을 보존하면서 시스템 로그/system logs에 syslog 검색 용어를 추가했다. 이는 원격 로그 전송 방식에 대한 답변 주장이 아니다. 관련 32개 테스트와 타입 검사를 통과했다. 네 개발군에서 실제 질문 정규화가 달라지는 것은 한국어 시스템 로그 1문항임을 확인했으며, 변경된 입력의 실제 추론과 전체 선택은 아직 남아 있다.
- 과거 공개 평가군의 전체 추론은 첫 질문에서 62.13초 후 시간 초과로 중단됐다. `full-calibration-exposed-client-timeout.json`은 불완전 결과이며 무응답 성공으로 계산하지 않는다. BLRO에서 기존 추론 종료와 가용 메모리 약 22GB를 확인했다. 60초 제한을 올리거나 반복 실행하지 않고 별도 loopback 서비스에서 CPU 추론 스레드 4개와 8개를 비교하는 진단을 시작했다. 모델/revision/1024토큰/float32/배치 4/지시문은 동일하다.

## 개발 설정 선택과 처리 시간 재측정

- 8스레드 별도 Qwen 서비스에서 시간 초과했던 첫 공개 질문은 30.71초에 완료됐다. 전체 공개 평가 12개 양성·12개 무응답도 이후 오류 없이 완료했다. 첫 질문 점수는 입력/후보 순서/검색 점수/모델 입력 해시를 확인한 진단 결과에서 재사용했고 원래 추론 시간과 원시 해시를 별도 기록했다. `full-calibration-exposed-client.json` 참조.
- 기존 4스레드 허구 질문 진단과 비교하면 75개 입력 해시는 일치했으며 그 점수 차이는 최대 약 0.0000391이었다. 나머지 112개는 입력 또는 순서가 달라 점수 비교에서 제외했다. 따라서 모든 점수 변화나 시간 개선을 CPU 스레드 하나의 효과로 일반화하지 않는다. `cpu-thread-score-comparison.json` 참조.
- 시스템 로그 용어 보완 후 실제 검색/모델 입력을 다시 확인했다. 기존 25개·영어 16개·한국어 15개 입력은 변경 전과 같았다. 한국어 시스템 로그 1개는 후보와 입력이 달라져 실제 Qwen 추론을 새로 실행했고 25.85초에 정답 2위를 확인했다. 원시 이전 결과를 덮어쓰지 않고 변경된 한국어 평가를 별도 구성했다.
- 보완 전 전체 점수 비교는 7,224개 설정 중 적격 0개였다. 기준을 유지하고 용어 보완을 반영한 비교에서는 적격 54개를 찾았다. 정책 v3에 따라 검색 순서 유지, 점수 하한 `3.666952610015869`를 개발 후보로 선택했다. 기존 20/21, 영어 11/12, 번역 한국어 11/12, 과거 공개 질문 11/12이며 각 무응답 오탐 0건이다. 이는 저장된 실제 점수의 오프라인 재평가로 최종 보류 평가나 운영 승인과 다르다.
- 선택된 설정의 실제 MCP 개발 read-back을 시작했다. 실제 제품 경로에서의 일치 확인과 설정 동결, 새 holdout-2 및 실제 답변/인용 검증이 남아 있다.

## 두 번째 고정 평가 결과: 정상 적중 통과, 무응답 실패

- `5e6f2cb`에서 모델/코드/코퍼스/점수 하한/정렬 및 평가 기준을 고정한 뒤 처음으로 client holdout-2를 실행했다. 구현 파일과 질문 해시가 고정 당시와 같음을 실행 전후 확인했다. 해당 커밋의 필수 CI 4개도 모두 통과했다.
- 정상 질문 12/12 적중, 무응답 오탐 4/12(33.3%), 금지 소스 0건, 누락 정답 소스 0건, degraded 질문 0건이다. 지연 중앙값 약 24.10초, p95 약 28.28초다. 무응답 기준 10%를 넘었으므로 **최종 후보는 미채택**이다. `holdout-2-client-frozen.json`과 `holdout-2-client-acceptance.json`에 원시 결과와 판정을 보존했다.
- 오탐 3건은 실제 고객 장비의 현재 로그인 사용자/공격자 신원/개인 키를 공개 매뉴얼로 답하려는 요청이다. 나머지 1건은 존재하지 않는 냄새 인식 패킷 필터를 일반 필터 문서에 연결했다. 원점수만으로 실제 환경 근거 유무나 요청된 기능의 모든 조건을 검증할 수 없다는 한계가 확인됐다.
- 이 결과를 기준으로 추가 개선하면 holdout-2는 개발 자료로 전환해야 한다. 같은 질문으로 튜닝 후 독립 검증 통과라고 주장하지 않는다. 다음에는 실제 환경 정보가 필요한 질문과 문서 사용법 질문의 경계를 먼저 보완하고, 새 평가 세트를 고정한 뒤 기준 변경 없이 재검증한다. LightRAG는 도입하지 않는다.
- BLRO에 `releases/5e6f2cb`를 별도 준비하고 아카이브 SHA `d06d51dd1b7a986aed7c36552b350336517922c14d4bce6cda407904c6a4bfc9` 일치를 확인했으나, 운영 실행 경로 전환은 하지 않았다. E5 8104와 Qwen 8109 검증 서비스는 응답했고 Control Tower 3700 readiness 엔드포인트는 연결되지 않았다. 실제 답변/인용의 최종 검증과 운영 readiness/전환은 미완료다.

## 실제 환경 근거 요구 분기와 세 번째 평가 고정

- 추가 개선 전에 새 평가 12개 양성·12개 무응답을 고정했다. `3d8d50c`에 manifest, `5c36a14`에 무시된 data 경로의 두 고정 fixture를 명시적으로 추적했다. 원본·구조 문서의 인용 12개를 검증했고 과거 정답 소스와 중복은 0이다. 같은 작성자의 합성 평가이고 문서 기반으로 고른 표본이라는 한계를 명시했다. 새 평가 결과는 아직 열지 않았다.
- 공개 문서 검색은 고객 인스턴스의 현재 값·상태를 직접 요구하는 질문을 `live-runtime` 근거 요구로 구분한다. 절차와 가정 시나리오는 검색을 유지한다. scoped 검색의 인증/권한 분기는 바꾸지 않았고, 잘못된 점수 설정을 정상 무응답으로 감추지 않는다. 규칙 기반의 제한된 분기이며 일반적인 답변 적합성 판정은 아니다.
- MCP summary와 콘솔에서 이유를 전달하고, 실제 상태 질문은 문서 수집용 search-gap에 저장하지 않는다. 관련 70개 테스트와 브라우저 3개 테스트, 타입 검사 통과.
- 이미 노출된 5개 평가군의 69개 양성 질문은 새 분기로 차단되지 않았다. 저장된 실제 검색 결과에 분기만 적용한 개발 재평가에서는 두 번째 holdout의 오탐이 4/12에서 1/12가 된다. 새 추론이나 독립 검증으로 주장하지 않는다. `runtime-requirement-development-replay.json` 참조.
- 실제 stdio MCP 호출에서도 기존 정상 질문 3개의 결과 순서가 같고, 기존 오탐 실제 환경 질문 3개는 빈 결과를 반환했다. `runtime-mcp-development-readback.json` 참조. 다음은 구현/설정 고정 후 새로운 holdout-3, 실제 답변/인용, 배포 검증이다.
