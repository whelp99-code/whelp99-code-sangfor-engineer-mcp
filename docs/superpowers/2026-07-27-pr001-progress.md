# Learning Strategy Observer 계획 완료 현황

최종 갱신: 2026-07-29
정본 계획: [2026-07-23-learning-strategy-observer.md](./plans/2026-07-23-learning-strategy-observer.md)  
통합 브랜치: `integration/pr001-security-followups`  
현재 코드 체크포인트: `3bc51d4` (IAG 13.0.120 strict live diagnosis와 전체 계획 재검토)

이 문서는 2026-07-27의 PR-001 중간 보고를 최종 통합 결과로 갱신한다. 코드 완료와 외부 장비 실증을 구분하며, 실행하지 않은 실증을 PASS로 기록하지 않는다.

> **2026-07-29 상태 정본:** [전체 계획 검토 및 개발 순서 체크리스트](./2026-07-29-full-plan-review-checklist.md)가 현재 구현 상태와 다음 개발 순서를 교정한다. 아래 2026-07-28 완료 판정은 당시 자동 인수 테스트 범위의 역사 기록이며, 모든 runtime integration과 dedicated REST/OpenAPI가 완료됐다는 뜻이 아니다.

## 최종 판정

- PR-001A~PR-012의 코드·자동 인수 기준은 완료됐다.
- REQ-01~REQ-24의 자동 검증 경계와 REQ-25의 정직한 pilot 상태 게이트가 통과했다.
- 실제 VPN·Sangfor 장비·FortiOS 8.0 랩·운영 CDP·PostgreSQL 접근이 필요한 pilot maturity는 자동 완료 범위가 아니다. 현재 매니페스트는 근거에 따라 `BLOCKED` 또는 `NOT_RUN`이다.
- push, PR, merge는 요청 범위가 아니므로 수행하지 않았다.

## 완료 커밋

| 범위 | 커밋 | 결과 |
|---|---|---|
| PR-001 보안 후속 | `08c6da5`, `30a3cd2`, `a5de996` | nonce 경쟁·registry/vendor/specVersion identity·Spec loader confinement 보강 |
| PR-004 보강 | `ab1963c`, `e9db026`, `2f226b4` | encrypted bundle 승격, writer/reader 통합, existing-CDP invariant와 실제 Chromium E2E |
| PR-010 | `f34496a` | local outbox, idempotent Prisma mirror, retry/DLQ, additive migration |
| PR-012 | `ac60ba2` | pilot manifest schema·외부 의존 probe·운영 런북·허위 PASS 거부 |
| PR-011 MCP | `65afc64` | exact resolver/service와 신규 MCP 8개, 77→85 tool |
| PR-011 CLI | `5bae23b` | `strategy`/`observe` CLI와 고정 exit code·secure sign·purge dry-run |
| 최종 tip (docs) | `a83bd7c` | completion 기록; 직전 코드 커밋은 signed promotion binding + capture ID 수명 보정 |

## 최종 검증 증거

| 명령 | 결과 |
|---|---|
| `pnpm test` | 95 files passed, 1 skipped; 656 tests passed, 2 skipped |
| `pnpm run lint` | exit 0 |
| `pnpm run build` | exit 0 |
| `pnpm run test:observer:e2e` | `ok:true`, forbidden attach 0, storage mutation 0, endpoint mutation delta 0, page/PID invariant true, `capture-bundle.v1` |
| `pnpm run smoke:mcp` | exit 0, 정확히 85 tools |
| `pnpm strategy -- list` | exit 0 |
| `pnpm observe -- purge --before 2026-01-01T00:00:00.000Z` | exit 0, `dryRun:true`, removed 0 |
| `prisma validate` (dummy URL) | schema valid |

전체 테스트가 생성한 `outputs/Sangfor_설정가이드_MCP.pptx` 변경은 task-owned worktree에서만 발생했고 검증 직후 기준 커밋 상태로 복원했다. 최종 worktree는 clean이다.

## 독립 검토

- 지정 Grok 세션 `term_92dea65a-edaa-44e9-b8ac-23528a0888cf`의 1차 최종 검토는 signed promotion의 정상 경로와 action binding 결손을 발견해 `NOT_APPROVED`였다.
- promote-binding 수정에서 exact strategy/revision/from-state/to-state binding과 회귀 테스트를 추가했고, 문서 tip은 `a83bd7c`다.
- 같은 Grok 세션의 재검토 결과: `APPROVE`, CRITICAL 0, HIGH 0. focused 25/25와 독립 probe가 통과했다.

## CHECKPOINT — 2026-07-29 계획-현실 재동기화

- **Baseline:** `a83bd7c` 계획 완료 보고
- **Current code checkpoint:** `3bc51d4` (`feat(iag): add verified live diagnosis workflow`)
- **후속 경로 보정:** `5257534`에서 learning strategy와 capture storage를 `data/` 정본 아래로 고정했다.
- **IAG 결과:** sanitized `iag-live-observation.v1` strict parser, 수동 read-only 진단 CLI, manual category anchor와 Open Auth/credential 분리, local retention baseline 문구를 구현했다.
- **검증:** focused `5 files / 20 tests PASS`, `pnpm run lint` PASS, `pnpm run build` PASS. 직전 전체 IAG remediation gate는 `97 files passed, 1 skipped; 664 tests passed, 2 skipped`였다.
- **상태 교정:** REQ-18과 REQ-20은 `DRIFTED`; REQ-11~14, 16~17, 19는 runtime 기준 `IN_PROGRESS`; dedicated REST/OpenAPI는 `UNTOUCHED`다.
- **다음 순서:** Fact→Spec production safety boundary → LM-08 HMAC/nonce와 no-fabrication → Fact 실행 모델 → dedicated REST/OpenAPI.
- **외부 실증:** CC/IAG/FortiOS/cinder/PostgreSQL pilot는 새 evidence 없이는 계속 `BLOCKED`/`NOT_RUN`이다.

## 외부 실증 상태와 다음 조건

정본 상태 파일과 운영 절차는 다음과 같다.

- [pilot manifest schema](../acceptance/learning-strategy-observer/pilot-manifest.schema.json)
- [외부 의존 BLOCKED fixture](../acceptance/learning-strategy-observer/fixtures/external-dependencies-blocked.json)
- [실행 전 NOT_RUN fixture](../acceptance/learning-strategy-observer/fixtures/not-run-before-window.json)
- [운영 런북](../LEARNING_STRATEGY_OBSERVER_RUNBOOK.md)

실제 pilot를 PASS로 전환하려면 런북에 정의된 접근 probe, confined evidence, SHA-256 digest, exact product/version identity와 승인 증적을 새로 수집해야 한다. 자동 테스트 통과만으로 pilot PASS를 만들 수 없다.
